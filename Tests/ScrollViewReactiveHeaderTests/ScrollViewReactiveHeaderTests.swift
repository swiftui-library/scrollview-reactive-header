    import XCTest
    @testable import ScrollViewReactiveHeader

    final class ScrollViewReactiveHeaderTests: XCTestCase {
        func testExample() {
            // This is an example of a functional test case.
            // Use XCTAssert and related functions to verify your tests produce the correct
            // results.
//            XCTAssertEqual(ScrollViewReactiveHeader().text, "Hello, World!")
        }
    }
